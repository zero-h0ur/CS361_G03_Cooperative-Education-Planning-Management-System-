{
  "name": "co-ed-compute-prototype",
  "version": "1.0.0",
  "private": true,
  "description": "Local compute-layer prototype for the CO-ED company directory",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
